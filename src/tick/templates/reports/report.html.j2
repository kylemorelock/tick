<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ checklist.name }} – Report</title>
</head>
<body>
  <h1>{{ checklist.name }}</h1>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Check</th>
        <th>Severity</th>
        <th>Result</th>
        <th>Notes</th>
        <th>Evidence</th>
        <th>Matrix</th>
      </tr>
    </thead>
    <tbody>
      {% for row in rows %}
      <tr>
        <td>{{ row.id }}</td>
        <td>{{ row.check }}</td>
        <td>{{ row.severity }}</td>
        <td>{{ row.result }}</td>
        <td>{{ row.notes or "" }}</td>
        <td>{{ row.evidence | join(", ") if row.evidence else "" }}</td>
        <td>{{ row.matrix or "" }}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</body>
</html>
