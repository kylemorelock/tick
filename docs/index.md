# tick

tick is a CLI tool for executing QA checklists defined in YAML. It guides testers through
each item, stores session state, and generates reports.

Start here:

- `getting-started.md` for setup and core workflows
- `extending.md` for adding new reporters and loaders
